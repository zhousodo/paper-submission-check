interface:
  display_name: "Paper Submission Check"
  short_description: "Run pre-submission quality checks on academic papers"
  default_prompt: "Use $paper-submission-check to check @paper.tex and @refs.bib before submission and return a structured issue report."
